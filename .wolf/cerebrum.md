# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-07-21

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

## Key Learnings

- **Project:** ccl-website
- AGENTS.md is deprecated as of 2026-07-21 — CLAUDE.md is the sole convention source.
- VITE_SHOW_WORK env flag toggles the Work chapter (SignatureLives) via src/lib/flags.ts.
- src/data/triquetra.ts is generated from .brief/branding/Triquetra_Fill.png by scripts/extract_triquetra.py — regenerate only if the brand mark changes.
- GA4 uses **Consent Mode v2** (hardcoded gtag snippet in index.html, id via `%VITE_GA_MEASUREMENT_ID%`). gtag.js loads with the page but `analytics_storage` defaults to denied; `config` runs `send_page_view:false` so NO cookieless ping fires pre-consent. Accept -> `gtag('consent','update',{analytics_storage:'granted'})` + explicit `gtag('event','page_view')`. consent.ts only holds storage + grant/deny/clearGaCookies helpers (no loader). Correct pre-consent test = 0 `/g/collect` + 0 `_ga*` cookies, NOT "0 google requests" (the library always loads).
- README + WorldCode.tsx Lighthouse numbers are scoped to the LIVE production URL for a released version; do NOT overwrite them with local `bun run preview` numbers (localhost has no CDN/HTTP2 — LCP reads ~0.9-1.0s local vs 0.7s live, perf ~2pt lower). Re-measure live only after a deploy. Adding gtag costs ~220ms TBT in the lab (~1-2 perf pts).
- Any inline link sitting in a block of body text needs a persistent (non-hover) underline or it fails Lighthouse `link-in-text-block` (WCAG 1.4.1 color-only). Bit the ConsentBanner Privacy link.
- verify scripts hard-code the local Chrome path (C:/Program Files/Google/Chrome/Application/chrome.exe).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-07-22] When placing canvas marks against a clearance boundary (viewport edge, text band), don't compute the placement bound as EXACTLY the boundary (`EDGE_L - half`) — a mark then lands with zero slack and a `>= boundary` verify check fails by a floating-point epsilon. Add a small PAD (~2px) so every mark clears each boundary by a real margin. See bug-067.
- [2026-07-21] Don't reveal a container with gsap `autoAlpha` (or otherwise toggle visibility:hidden) if you need to `focus()` a control inside it in the same tick — a visibility:hidden ancestor makes the element unfocusable, so focus silently no-ops. Use `opacity` for the reveal, or move focus in the tween's onComplete.
- [2026-07-21] A keydown-based focus trap (intercept Tab on the container) CANNOT contain focus across a cross-origin iframe (e.g. a reCAPTCHA checkbox) — keydown/Tab events inside the frame don't bubble to the parent, so focus leaks out. Use focus SENTINELS instead: an empty `tabIndex={0}` guard element as the first and last child of the dialog whose `onFocus` bounces focus to the opposite end. Same reason, Esc-to-close won't fire while focus is inside the cross-origin frame (acceptable; Close button + backdrop still work).
- [2026-07-21] When computing a focus trap's first/last focusable, filter out elements that are hidden (`el.offsetParent === null`) or `tabIndex === -1`. Third-party widgets (reCAPTCHA injects a hidden `textarea[name=g-recaptcha-response]`) and honeypots (`display:none`, tabindex -1) otherwise get picked as the "last" focusable, and `.focus()` on a hidden element no-ops — so the wrap silently fails.
- [2026-07-21] `grecaptcha.render()` throws "reCAPTCHA has already been rendered in this element" under React StrictMode (dev double-invokes effects on the same DOM node). Guard the render with a `renderedRef` (render once) — do NOT let the render promise's `.catch` blindly set an error state, or the benign dev double-render trips it (prod has no StrictMode so it only breaks dev/verify). Also: a captcha solve fires inside a cross-origin iframe and causes NO React re-render — always drive the submit gate from a state value set by the widget's `callback`, never by polling `grecaptcha.getResponse()` at render time.
- [2026-07-21] Two overlapping `backdrop-filter` layers + unmounting the top one = Chromium drops the lower layer's blur until a repaint (seen: closing the hello drawer killed the nav's blur until next scroll). Don't stack backdrop-filters; the drawer backdrop uses a plain translucent `bg-navy/75` instead of `backdrop-blur-sm`. Separately, ST+Lenis desync after `scrollIntoView`+programmatic scroll is a harness artifact (reproduces with no drawer) — don't chase it as a product bug.
- [2026-07-21] Two sibling elements at the same position in a `{cond ? <div> : <div>}` swap are RE-USED by React (same tag → same DOM node). If you animate one with gsap (leaving inline `opacity`/`visibility`/`transform`), those styles carry over to the other branch. Symptom seen: the brief opened invisible/unclickable because the CTA's fade-out `autoAlpha:0` stuck to the reused node. Fix: give the branches distinct `key`s so each mounts fresh. Diagnostic tell: `getBoundingClientRect` reports a box but `document.elementFromPoint` at that box returns an ancestor, and synthetic `el.click()` works while real `page.click`/`page.type` don't (hidden elements reject real events but accept synthetic ones).
- [2026-07-21] CORRECTION to the note above: that "scroll past position → class dropped" was NOT just a harness artifact — it was a real bug. Never give an always-on scroll-STATE trigger an upper bound. A `ScrollTrigger.create({ start, end, toggleClass })` DEACTIVATES past `end:"max"` and toggleClass strips the class at the bottom (worse when the cached max is stale after page-height changes). For "scrolled past N" styling use an unbounded `start:0 end:"max"` with a manual `onUpdate/onRefresh` toggle on `self.scroll() > N`, set the initial state on mount, and DON'T gate it behind prefers-reduced-motion (scroll-state styling ≠ motion). See Nav.tsx / bug-016.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- [2026-07-21] World Logic (03) reworked from UI-fragment assembly (fake emails/names/metric cards) to blueprint-construction of the triquetra: a scrubbed technical drawing where dashed guides draw in, the three loops trace over them, then fills whisper in with the guides fading back. Now all three worlds resolve into the mark (01 comet draws it, 02 matrix assembles it, 03 constructs it). No personal data or text lives inside the visuals. Data attrs: data-blueprint, data-guide, data-loop, data-tick; drag interaction moved from [data-fragment] to [data-loop] (loop springs back with back.out(1.7)). verify6.mjs previously had zero Logic-world coverage — added a structural check (guides=5, loops=3, ticks=12, fragments=0, no personal text, loops grabbable when assembled).
- [2026-07-21] World Logic drafting-detail pass: added a plotter head riding the drawing tip (data-plotter, driven from the scrub onUpdate via self.animation.time(); loop trace ease switched to "none" so a linear tip sample matches the reveal), a dimension/annotation layer (data-dim: dim lines with end ticks, radius leaders, a 120° arc, mono labels "1:1"/"R1-3"/"120°" — abstract drafting notation only), four registration marks (data-regmark) framing the SVG sheet corners, and per-loop lock-in pulses. Key GSAP call: each guide circle's resolve-fade is FOLDED INTO its pulse keyframe (0.35→0.8→0.14 in one tween at loop-completion) so no second tween touches strokeOpacity on the same target — keeps it scrub-reversible. Regmarks sit at viewBox corners (the drawing square), not the full-width stage rect, because they live inside the square SVG.
- [2026-07-21] Inline brief flow (src/components/BriefForm.tsx, wired into Invitation.tsx): the "Start your brief" CTA opens a 3-question flow in place (no modal/route). Backend = Formspark POST to https://submit-form.com/${VITE_FORMSPARK_FORM_ID} (flag read via src/lib/flags.ts, empty → mailto: fallback to hello@creativecodelogic.com so the form never dead-ends). Honeypot field _gotcha. Height stability: Invitation reserves a fixed swap box (min-h-[22rem] md:min-h-[19rem]) sized to the tallest state so open==closed==success height and the footer never shifts — this deliberately makes the CLOSED page ~238px taller than pre-feature (a form taller than a button cannot both leave closed height untouched AND not shift the footer on open; chose footer stability). GOTCHA: step-reveal animation must use `opacity` not `autoAlpha` — autoAlpha sets visibility:hidden at the from-state, which makes focus() fail to land on the step's first control. verify6 section 6 walks all three steps (keyboard + click), asserts focus management + submit-disabled-until-valid, and STOPS before submitting (no network in CI).
- [2026-07-21] Invisible reCAPTCHA v2 + hello drawer. Captcha: src/lib/captcha.ts lazy-loads recaptcha api.js (explicit render) ONCE on first form open (brief or drawer), renders an invisible widget; src/lib/submit.ts is the shared Formspark path (honeypot→success, empty FORM_ID→mailto, else POST with g-recaptcha-response when a token is available; token "" on any captcha failure so nothing hangs). Flag VITE_CAPTCHA_SITEKEY via flags.ts — empty skips captcha. Badge hidden via .grecaptcha-badge{visibility:hidden} in index.css; required attribution rendered inline (src/components/CaptchaNotice.tsx, self-guards on sitekey). Payloads tagged form:"brief" / form:"hello" to distinguish in the Formspark inbox. Hello drawer (src/components/HelloDrawer.tsx): portal to document.body, z-[60] (nav is z-50), role=dialog aria-modal, focus trapped (Tab/Shift-Tab cycle), Esc + backdrop close, focus returns to [data-hello-open] trigger, scroll locked via new stopScroll()/startScroll() in scroll.ts (lenis.stop/start + documentElement overflow hidden). Third-party recaptcha console noise is filtered in verify6's watch() so it doesn't fail the zero-errors gate.
- [2026-07-21] Contact stack restructure (supersedes parts of the entry above). TWO Formspark forms now: FORMSPARK_FORM_ID_BRIEF (VITE_FORMSPARK_FORM_ID_BRIEF) + FORMSPARK_FORM_ID_CONTACT (VITE_FORMSPARK_FORM_ID_CONTACT); the old single VITE_FORMSPARK_FORM_ID is gone. submit.ts now takes (formId, payload, mailtoHref|null, token) — no captcha object. BriefForm → BRIEF form, NO captcha at all (no script load, no attribution). HelloDrawer → CONTACT form with a VISIBLE reCAPTCHA v2 checkbox (captcha.ts renderCheckbox: size normal, theme dark, explicit render, lazy-loaded on first drawer open only; token via grecaptcha.getResponse, reset after submit + on close). Send gated on valid fields AND captchaSolved. CaptchaNotice.tsx and the .grecaptcha-badge CSS were DELETED (visible widget carries its own Google branding). No email addresses in the contact UI: drawer "Prefer email?" line removed, error state is "Something broke on the way. Give it another try." + a Try again button (no mailto). BriefForm keeps its mailto fallbacks (different form). The invisible-captcha flow (mountInvisibleCaptcha) was fully removed.
- [2026-07-21] Mobile menu (src/components/MobileMenu.tsx, wired in Nav.tsx). Full-screen navy overlay, portal to body, z-[60] (nav z-50); reuses the hello-drawer playbook — role=dialog aria-modal, sentinel focus trap (not keydown-only), Esc close, stopScroll/startScroll lock, opacity (never autoAlpha) so links stay focusable, focus returns to the hamburger. The animated × lives in the OVERLAY top-right (inside the aria-modal dialog + focus trap), NOT the nav hamburger morphing in place — the overlay covers the nav, so morphing a hidden hamburger would be invisible and a close control outside the dialog is wrong for screen readers. Hamburger is right-aligned beside the "Start a project" pill, md:hidden, 44px. Links reuse the SHOW_WORK-filtered LINKS (Work stays conditional). Link click closes FIRST then rAF scrollToId (scroll must restart before scrolling). Nav.tsx also portals it like Invitation portals HelloDrawer. Site-wide: added focus-visible:ring-2 ring-accent to nav links/logo/CTA, hero CTAs, palette swatches. Palette swatch hit area is now a 40px button wrapping the unchanged 16px dot (inner span; group-hover for the hover opacity) — verify checks button offsetWidth 40 / dot offsetWidth 16.
- [2026-07-21] World Code terminal (WorldCode.tsx BUILD_LOG + METRICS) shows ONLY real measured values — the brand pitch is "judge it now", so it must never fabricate. It previously faked "tests 6/6 passed" (no test suite), "lighthouse audit — no issues", "deployed to production", Lighthouse 100 / LCP 0.8s. Now measured 2026-07-21 on the production build served locally (bun run build && bun run preview :4173; bunx lighthouse http://localhost:4173 --preset=desktop, CHROME_PATH = the verify Chrome): vite 8.1.5, 45 modules, tsc clean, verify6 3/3 viewports 0 errors, Lighthouse desktop performance 98 / LCP 0.7s (a11y 96, best-practices 100, SEO 91). Log lines + metric targets reflect exactly that; a comment block in the file records the re-measure command + a note to re-run against the real production URL after deploy (localhost differs). RULE: never write build/perf claims in UI you haven't just measured; re-measure after deploy.
- [2026-07-21] Pre-launch pass. Fonts self-hosted (no fonts.googleapis/gstatic runtime dep): downloaded latin woff2 to public/fonts/ — they turned out to be VARIABLE fonts (one file per family covers all weights; byte-identical across requested weights), so index.css has 3 @font-face with weight RANGES (Inter 400 500, JetBrains Mono 400 500, Space Grotesk 500 700), font-display:swap, url("/fonts/*.woff2"). index.html preloads /fonts/space-grotesk.woff2 (hero h1 = Space Grotesk 600, within the range) with crossorigin. @theme font tokens untouched. Re-download cmd: fetch https://fonts.googleapis.com/css2?family=... with a Chrome UA, grab the /* latin */ block's woff2. SEO: index.html got canonical, og:url/site_name, absolute og:image (https://creativecodelogic.com/og.jpg), twitter summary_large_image, and JSON-LD Organization (name/url/logo=favicon.svg — only real data, no invented sameAs/address). public/robots.txt (allow all + Sitemap line) and public/sitemap.xml (single URL) added. Production origin https://creativecodelogic.com (NON-www assumed canonical — if www wins it's a find-replace). A11y fixes: nav logo aria-label now "CCL — Creative Code Logic, back to top" (label-in-name: visible "CCL" must be contained); terminal header text-mist/60 → text-mist/80 (contrast). Lighthouse desktop after: perf 95 (stable 94-96), LCP 0.6s, accessibility 100, best-practices 100, SEO 100 (was 98/0.7/96/100/91). WorldCode terminal numbers updated to 95 / 0.6s to stay truthful.
- [2026-07-22] Verify VISIBILITY with pixel readback (canvas getImageData → count alpha>0), not DOM state — a canvas can pass every DOM assertion (present, z-index, aria, pointer-events) while completely hidden behind an opaque layer. The AmbientField shipped "passing" but invisible for exactly this reason.
- [2026-07-22] Negative z-index elements paint BELOW body's own background when html ALSO has a background. If html has background-color, body's background does NOT propagate to the page canvas — it becomes an opaque element layer above z-[-N] children. Rule: put the page background on html only (or ensure no body background) when you want a z-[-N] layer to show. Fixed by removing body's background-color (html keeps navy). See bug-019.
